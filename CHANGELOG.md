# CHANGELOG

## [Unreleased] - 2026-07-25

### Added
- **顧客選択連動型 自動サブフォルダ画像探索＆取得元フォルダ名動的更新エンジンの完成**
  - **顧客切り替え時の画像フォルダ自動連動**: 画面で顧客（例: `16152_トーベイ（株）.xlsx` や `27099_（株）みどりフーズ.xlsx`）を選択・切り替えた際、アプリが自動的に同名の顧客サブフォルダ（`16152_トーベイ（株）` や `27099_（株）みどりフーズ`）を検出・照合。
  - **「取得元: 顧客フォルダ名」のリアルタイム更新**: ヘッダーの「画像接続済」直下に表示される取得元バッジが、選択中の顧客名（例: `取得元: 16152_トーベイ（株）`）に連動してリアルタイムに動的切り替えされるように完全対応。

### Fixed
- **スマホ環境での画像追加接続時の商品カード再描画ブロック修復 (`ProductCard.jsx`)**
  - **`React.memo` 再描画判定の修正**: スマホで「画像フォルダ」ボタンを押して画像を接続した際、`ProductCard` の memo化比較関数が `imageFilesMap` の変化を検知せず画面の再描画（Re-render）をブロックしていたバグを修復。画像接続完了と同時に全商品カードが即座に最新画像付きで再描画されるように改善。

### Fixed
- **`44884A.jpg` 等のアルファベット枝番付き画像に対する純粋数字キー (`44884`) 抽出・相互バインディングの追加修復**
  - **純粋数字ベースキーの自動登録**: `44884A.jpg` などのファイルが読み込まれた際、末尾のアルファベット枝番 `A` を除去した「純粋受注№ (`44884`)」を自動抽出し `imageFilesMap` にベース登録。Excelデータの受注№が `44884`（枝番なし）の場合でも確実にヒットするように改善。
  - **商品コード（`productCode`）フォールバックの追加**: 受注№で画像がヒットしない場合でも、商品コード（例: `008240501.jpg`）での自動検索フォールバックを組み込み、画像の読み取り漏れを完全解消。

### Documentation
- **知見・教訓および開発ルールのドキュメント化 (`LESSONS_LEARNED.md`, `.agents/AGENTS.md`)**
  - **行動教訓（推測の完全排除）**: 外部認証やデータ読み取り時の推測による回答を排除し、一次情報の画面確認を徹底する行動ルールの定義。
  - **Google Drive実データ構造**: 顧客サブフォルダ構造（`16152_トーベイ（株）`）および画像命名仕様（`44884A.jpg`）の技術的記録。
  - **スマホ技術制約と最適化パターン**: File System Access API代替インプット、O(1)ハッシュ検索、非ブロッキングIndexedDB保存、アコーディオンUIパターンの体系化。

### Fixed
- **Google Drive実データ（`16152_トーベイ（株）` ＆ `44884A.jpg`）に完全最適化した画像読み取り・探索エンジンの完成**
  - **`44884A.jpg` 等のアルファベット枝番の全自動補完・探索**: 受注№ `44884` に対し、実在する `44884A`, `44884B`, `44884C`, `44884a` などの大文字・小文字枝番サフィックスを完全網羅して最優先探索するよう `imageKeyUtils.js` および `ProductImage.jsx` を強化。
  - **顧客サブフォルダ（`16152_トーベイ（株）/`）内画像の最優先ピンポイントヒット**: Google Drive 内の実在顧客サブフォルダ名およびアンダースコア連結（`16152_`）での優先探索キー照合を確定。一覧の読み込みと表示がPC・スマホ問わず完璧に一致。

### Fixed
- **スマホ環境でのアンダースコア区切り画像ファイル名 (`27099_1005235.jpg`) の自動解析と照合修復**
  - **自動分解・受注№抽出キー登録**: スマホで画像ファイルを選択した際、`27099_1005235.jpg` や `みどりフーズ_1005235.jpg` のようにアンダースコア `_` やハイフン `-` で連結されたファイル名から「受注№単体 (`1005235`)」を自動分解・抽出しキー登録。
  - **アンダースコア連結パターンの探索追加**: `ProductImage.jsx` での探索時に `27099_1005235.jpg` などの連結パターンも優先照合候補に追加し、`27099_（株）みどりフーズ.xlsx` を開いた際一気に画像が表示されるように修復。

### Added
- **スマホ環境専用「画像取得元フォルダ名」表示バッジの追加**
  - **「画像接続済」下の取得元フォルダ表示**: スマホ環境（`isFileSystemSupported === false`）で画像が接続されている際、「画像接続済」ボタンのすぐ下に現在接続している画像の取得元フォルダ名（例: `取得元: 16152_トーベイ`）を省スペースなバッジで自動表示する機能を追加。

### Fixed
- **スマホ環境での「選択中顧客サブフォルダ内画像」最優先照合の実装**
  - **顧客サブフォルダ優先ヒット**: スマホの画像探索時、現在選択中の顧客ファイル名から抽出した「`顧客フォルダ名/受注No`」および「`顧客コード/受注No`」（例: `16152_トーベイ/1005235.jpg`, `16152/1005235.jpg`）を最優先でピンポイント照合・検索するように修正。他顧客フォルダ内の同名画像の混同や検出漏れを防止。

### Fixed
- **スマホ環境での「画像フォルダ / 画像選択」ボタン不発および画像表示不具合の完全修復**
  - **スマホでの画像ダイアログ優先発火**: スマホ環境（`!isFileSystemSupported`）で「画像フォルダ」ボタンを押した際、スマホで無効化される `webkitdirectory` 属性付き input が呼ばれてダイアログが開かない現象を修復。`image-files-input` を優先起動してスマホから画像を取り込めるよう修正。
  - **PC同等の画像探索照合**: スマホの `imageFilesMap` キー登録および `ProductImage.jsx` での照合ロジックを拡充し、元ファイル名・拡張子付き/なし・ゼロパディング等の全キーでPCと同等にマッチング・表示できるように強化。

### Added
- **スマホUI「顧客・直送先選択」親アコーディオン化と折りたたみ対応**
  - **1つの親アコーディオンへ統合**: 「顧客選択」と「直送先選択」を1つの親パネル「顧客・直送先選択」としてまとめ、タップ操作で開閉（折りたたみ/展開）できるように変更。
  - **選択中サマリーバッジの追加**: パネルが折りたたまれている状態でも、現在選択されている顧客名および直送先名をヘッダーにバッジ表示。
  - **閲覧領域の省スペース化**: パネルを折りたたむことで、スマホ画面での商品一覧カード・表示エリアを最大化。

### Fixed
- **スマホ環境での「顧客フォルダ / 顧客ファイル選択」および自動読み込み不具合の修復**
  - **スマホでのファイル選択ダイアログ起動修正**: スマホ等の `showDirectoryPicker` 非対応環境において、`<input webkitdirectory>` が誤ってトリガーされ選択ダイアログが開かない不具合を修正。`customer-files-input`（複数ファイル選択）を優先起動するように変更。
  - **選択直後の自動読み込み**: スマホで顧客ファイルを選択した際、先頭の顧客ファイルを即座に `processExcelFile` で読み込むように改善（「データを読み込んでください」の画面で止まる現象を修正）。
  - **顧客ファイル照合の柔軟性強化**: `loadCustomerFile` 内で拡張子省略（`.xlsx`, `.xls`）や顧客コード（`22072`等）によるマッチングを多重化し、ファイルが見つからないエラーを防止。

### Performance
- **IndexedDB書き込みの非ブロッキング・バックグラウンド化（もっさり感の完全解消）**
  - **`productData` / `customerFilesCache` の非同期保存**: 重いデータやファイル名リストの IndexedDB への保存を `await` せずバックグラウンド（`Promise`）で処理することで、データ解析完了と同時にUI（画面表示）が即座に切り替わるように最適化。
  - **`File` オブジェクト書き込み排除**: モバイル環境向け `customerFilesCache` に `File` オブジェクトをシリアライズする遅延を排出し、軽量なファイル名リストのみをバックグラウンド保存。
- **スマホ環境での画像読み取り表示ラグの解消（高速化・省メモリ化）**
  - **全件スキャン O(N) の完全削除**: `ProductImage.jsx` で発生していた `imageFilesMap.entries()` の全件ループ処理を廃止し、受注Noのみを対象とした正規化キーによる O(1) ハッシュ検索に変更。
  - **受注Noのみに限定した画像検索の標準化**: 画像ファイルのマッチングを受注No単体に特化。無駄なフォルダ名バリエーションキーの生成をカットし、メモリ消費量を削減。
  - **顧客ファイル照合のコード優先化**: 「`22072_（株）中尾米穀店`」などの顧客ファイル・フォルダを先頭の顧客コード番号（`22072`）で一致させるロジックを共通化（`extractCustomerCode`）。
  - **Blob URL キャッシュの導入**: `WeakMap` による `getOrCreateObjectURL` キャッシュを導入し、同一ファイルに対する重複 `URL.createObjectURL` 生成とスクロール時の描画オーバーヘッドを激減。

## [Unreleased] - 2026-07-24

### Fixed
- **画像検索キーの「受注№」限定化およびPC・スマホ環境別フォルダ仕様の標準化**
  - 画像の検索キーを「受注№」（`filename`）単体に完全に一本化し、商品コード等による不要・混同する検索ロジックを排他。
  - **PC環境（直下一括構造）**: 選択した画像フォルダの直下に全画像が保存されている運用に対応。余計なサブフォルダ検索を一切行わず、受注№で直引き（O(1)アクセス）して一瞬で表示。
  - **スマホ環境（顧客別サブフォルダ構造対応）**: 得意先別サブフォルダ（`16152_トーベイ/1262418.jpg`等）を含む画像選択に対応。選択中の顧客（`16152`等）と「受注№」を自動照合し、スマホ側でも高速表示。
  - 上記の検索ルールおよび環境別仕様を `README.md` に明記・更新。

### Changed
- **DESIGN.md (デザインシステム v1.0.0) の全面適用**
  - `:root` に公式トークン (`--color-primary`, `--color-dark`, `--rounded-md`, `--spacing-md` 等) を定義し、アプリ全体のCSS変数を統合。
  - ヘッダー・検索バー・プライマリーボタン・カード・モーダル・バッジのスタイルを `DESIGN.md` 仕様へ完全準拠。
  - **モバイルUIの全面リニューアル**: モバイルヘッダー、検索バー、モバイルカード、タッチ時のアクティブフィードバック、モバイル用カート・ダイアログに `DESIGN.md` のトークンとアニメーションを適用。
  - **モバイル個体画面サイズ・画角への完全対応**: Dynamic Viewport Height (`100dvh`), `viewport-fit=cover`, Safe Area Insets (`env(safe-area-inset-*)`), iPhoneのノッチ・画面下部ホームバー・ブラウザ可変バーへの自動フィッティング構造を導入。
  - **モバイルヘッダー途切れ・画面横崩れの修正**: `html, body, .amazon-app` への `overflow-x: hidden` および `max-width: 100vw` の適用、モバイル用ヘッダーのアクションボタンのレイアウト最適化により、ヘッダーが途切れる現象を追放。
  - **商品カード2列100%表示＆フィルター部・顧客選択の見切れ修正**: 商品カードを2列固定 (`repeat(2, 1fr)`) で画面幅100%にフィッティング。「顧客選択」「直送先選択」「フィルターパネル」の横溢れ原因を排除し、画面幅内に100%確実に収まるよう完全修復。
  - **商品画像下の無用な縦余白の徹底削減とレイアウト最適化**: 画像容器の固定高さを `120px` に縮小し、画像〜本文タイトル間の無駄な空洞を排除。各要素間・ツールバーのパディングをコンパクトに引き締め。
  - **ヘッダー下キャッシュボタン〜顧客選択パネル間の無駄な上部余白の完全解消**: PC用スタイル `custom.css` 内にあった `.amazon-sidebar` の `top: 90px;`（ヘッダー下90pxの位置に固定する設定）がモバイルで誤適用されていた原因を突き止め排除。インポート順 (`main.jsx`) の最適化とモバイル上部マージンのゼロリセットにより、キャッシュボタン直下に顧客選択パネルがジャスト配置されるよう完全解決。
  - 各CSS (`custom.css`, `mobile.css`, `mobile-table.css`, `product-details-modal.css`) および JSX コンポーネント内の直書きカラーコードをデザインシステム変数へリファクタリング。
- **サイドバー全域のUI/UXリニューアルと操作性の向上**
  - セクション全体を丸みのあるモダンな白カードパネル（角丸 `12px` ＋ シャドウ ＋ 薄いボーダー）で再構築。
  - **顧客名・直送先名の見切れ（`...`）解消**: 顧客コード（`[16152]` など）を専有バッジ化し、名称部を2行折り返し表示に変更。選択中項目にはチェックマーク（✓）と鮮やかなアクセント背景を適用。
  - **検索ボックスの利便性向上**: 顧客名検索・直送先名検索の入力欄に「✕（一括消去）」ボタンを追加。1クリックでキーワードリセットが可能。
  - **アクティブフィルタータグ（チップ）エリアの新設**: サイドバー下部上方に、現在選択中の条件を `[ PET ✕ ]` `[ 既製品 ✕ ]` のように一覧表示。1タップで個別条件を解除できる領域を追加。
  - **属性フィルターの視認性向上**: アコーディオンヘッダーに直感的なアイコン（🏷️ ⚖️ 🧪 🎨）を追加し、該当件数 `(12件)` を右寄せバッジ化。

### Fixed
- **PWAのリソース404エラーおよびスクリプト実行拒否エラーの修正**
  - デプロイ環境（Vercel）等で `/registerSW.js` および `/manifest.webmanifest` が404エラーとなりスクリプト実行が拒否される問題を解消。
  - [public/manifest.webmanifest](file:///c:/gemini_cli/得意先別商品検索ツール/public/manifest.webmanifest) および [public/registerSW.js](file:///c:/gemini_cli/得意先別商品検索ツール/public/registerSW.js) を新規作成し、[index.html](file:///c:/gemini_cli/得意先別商品検索ツール/index.html) に適切なマニフェストリンクと登録処理を設定。
- **PWAの古くなったキャッシュ保持（Cache-First）問題の修正と更新機能の追加**
  - Service Worker（[public/service-worker.js](file:///c:/gemini_cli/得意先別商品検索ツール/public/service-worker.js)）のキャッシュ戦略を従来の Cache-First から **Network-First** に変更。これによりオンライン時は常に最新のコード（HTML/JS/CSS）をサーバーから取得し、キャッシュを動的更新。
  - Vercelの設定（[vercel.json](file:///c:/gemini_cli/得意先別商品検索ツール/vercel.json)）に `Cache-Control: no-cache, no-store, must-revalidate` を追加し、HTML/SWファイルのCDNキャッシュによる停滞を完全に防止。
  - キャッシュ管理画面（[CacheManager.jsx](file:///c:/gemini_cli/得意先別商品検索ツール/src/CacheManager.jsx)）に「最新版に強制更新（全キャッシュ削除）」ボタンを追加。ワンクリックでService Worker登録解除・Cache Storage全削除・IndexedDB削除を一括実行し最新状態に更新可能に改善。

### Performance
- **ビルドプロセスの最適化と高速化**
  - `lucide-react` の全アイコンバレルインポートによるビルド時のモジュール数膨大化（1717モジュール）およびWindows上でのメモリクラッシュを改善。
  - 個別アイコンインポートを集約した [src/icons.js](file:///c:/gemini_cli/得意先別商品検索ツール/src/icons.js) を新設し、[vite.config.js](file:///c:/gemini_cli/得意先別商品検索ツール/vite.config.js) にてエイリアスおよび `vendor-exceljs`, `vendor-xlsx`, `vendor-react` などの `manualChunks` 分割設定を構築。ビルド時間を約3.5秒に短縮。

## [Unreleased] - 2026-07-22

### Changed
- **Excelエクスポート時の商品画像出力の廃止**
  - 得意先向けExcelファイルにおいて、画像ファイルをセルに埋め込まない仕様に変更。
  - 「商品画像」列（B列）自体を完全に削除し、出力される列を整理（列数を13列から12列へ削減、マージ範囲を A1:L1 と A2:L2 に調整）。
  - 画像非表示に伴い、Excelの各行の高さ（`row.height`）を画像用（80px）からテキストが綺麗に収まる高さ（25px）に調整。
  - Excel生成中トーストメッセージから「画像点数により時間がかかる場合があります」という案内を削除。
  - ユニットテスト（[excelExporter.test.js](file:///c:/gemini_cli/得意先別商品検索ツール/src/utils/excelExporter.test.js)）を更新し、画像列削除に伴うセルの配置やインデックス（列のずれ）の検証内容を修正。

### Added
- **直送先選択のキーワード検索機能の追加**
  - サイドバー内の「顧客選択」セクションのすぐ下に、新しく「直送先選択」セクションを追加。
  - 顧客選択と同様に、テキストキーワード入力（`Search` アイコン付きインプット）による直送先の動的なフィルタリング機能（部分一致検索）を実装。
  - 直送先項目をクリックすることで、トグル形式による複数選択（フィルタ適用）が可能なUIを構築。
  - アコーディオン式フィルター一覧から「直送先名称」を除外し、サイドバーのレイアウトを整理。
  - 「直送先選択」ヘッダー横に、直送先フィルタのみを一括クリアできる「クリア」ボタンを追加。また、顧客切り替え時や「すべてのフィルターをクリア」実行時に直送先検索窓も連動して自動リセットされる仕組みを導入。
- **リロード後も接続が維持される顧客フォルダ・ファイルのキャッシュ永続化**
  - **モバイル環境での永続化**: スマホ等で一括選択された顧客Excelファイル（`File` オブジェクト）自体を IndexedDB 内にキャッシュ (`customerFilesCache`) する仕組みを新設。リロード（更新）してもファイル選択が解除されず、再選択なしに顧客を切り替え可能に改善。
  - **PC環境での接続維持**: リロード後にブラウザ側のセキュリティ制限でフォルダアクセス許可（パーミッション）が一時的に切れている場合でも、以前の顧客ファイル名リスト（`customerFilesListCache`）をキャッシュから復元して画面に表示させ続けるよう改善。
  - **オンデマンドな権限要求の導入**: 顧客リストの項目をクリックした時点で初めてオンデマンドでパーミッションを要求（`requestPermission`）し、許可が得られたら動的にExcelデータを処理・読み込む体験を構築。更新のたびに再接続させられる手間を排除。
  - キャッシュ管理（`CacheManager.jsx`）での「商品データを削除」時に、今回新設した顧客接続関連のキャッシュ（`customerDirHandle`、`customerFilesCache`、`customerFilesListCache`）も一括でクリーンアップするよう処理を追加。

## [Unreleased] - 2026-07-17

### Added
- **お客様提出用HTMLエクスポート機能の追加（単価・印刷代なし）**
  - 現在表示されている（検索・フィルターで絞り込まれた）商品データを、見栄えを整えたHTMLファイルとしてエクスポート・ダウンロードする機能を追加。
  - 得意先への提案用途を考慮し、「単価」および「印刷代」を完全に除外したレイアウトを適用。
  - ダウンロードされた単一のHTMLファイルをオフラインや別PCで開いても正しく画像が表示されるよう、商品画像を非同期で読み込み Base64（データURL）化してファイル内に直接埋め込む機能を実装。
  - 游ゴシックフォント、ミッドナイトブルーヘッダー（`#1e293b`）、ゼブラ柄の背景、ホバー強調などのプロレベルのCSSスタイルを埋め込み。
  - **拡大プレビュー（モーダル機能）の搭載**: 画像をクリックすると、半透明の美しいぼかし背景とともに拡大画像がズームアップ表示されるライトボックスモーダルを自己完結実装。
  - **デザインの調整**: 閲覧時の一体感を向上させるため、データ行の「品名」セルの配置を左寄せから中央揃えに変更。
  - 新規ロジックを [htmlExporter.js](file:///c:/gemini_cli/得意先別商品検索ツール/src/utils/htmlExporter.js) に記述し、[htmlExporter.test.js](file:///c:/gemini_cli/得意先別商品検索ツール/src/utils/htmlExporter.test.js) を追加して単体テスト（2件）で価格情報が除外されていることを検証。
- **お客様提出用Excelエクスポート機能のプロ品質化と画像埋め込み**
  - エクスポートライブラリを `exceljs` に移行し、セルの細かな罫線（グリッド線）やフォント、配置（中央揃え・右揃え・左揃え）などをプロレベルの品質に調整。
  - **デザインと可読性の更なる向上**: すべてのフォントを「游ゴシック（Yu Gothic）」に統一。偶数行にごく薄いグレー（`#F8FAFC`）の背景を設定したゼブラ柄（ストライプ）デザインを採用。罫線を薄いグレー（`#E2E8F0`）の細い罫線に変更し、全体的に列の余白（パディング）を `+5` 文字分に広げて見やすさを向上。日付表示を `YYYY/MM/DD` に統一。
  - 「商品画像」列（B列）を新設。キャッシュやローカルサーバー、ローカル画像フォルダから非同期で商品画像を読み込み、アスペクト比を維持してセルに自動埋め込みする機能を実装（行高は自動的に80pxへ調整）。
  - 共通の非同期画像取得処理を [imageLoader.js](file:///c:/gemini_cli/得意先別商品検索ツール/src/utils/imageLoader.js) に分離・カプセル化し、Node.jsテスト環境での indexedDB 存在チェックを追加して ReferenceError 警告が発生しないようにクリーンアップ。
- **お客様提出用の商品一覧Excelエクスポート機能の追加**
  - 現在表示されている（検索・フィルターで絞り込まれた）商品データを、見栄えを整えたExcelファイルとして生成・ダウンロードできる機能を追加。
  - Excelファイルのタイトル行や出力日の自動挿入、列幅の自動調整、数値列（単価、印刷代）に対する通貨表示フォーマット（`¥#,##0`）の適用。
  - テストおよびメンテナンス性を考慮し、データ構築用ロジックを [excelExporter.js](file:///c:/gemini_cli/得意先別商品検索ツール/src/utils/excelExporter.js) に分離・カプセル化し、[excelExporter.test.js](file:///c:/gemini_cli/得意先別商品検索ツール/src/utils/excelExporter.test.js) にて単体テスト（2件）を追加。

### Fixed
- **モバイル（スマートフォン）表示向け UI の最適化**
  - 画面幅が 768px 以下の環境（スマートフォンや縦向きタブレット等）で「テーブル表示」を選択した際、画面外へテーブルがはみ出して崩れてしまう不具合を修正。
  - PC版のテーブルと並行して、縦スクロールでタップしやすい「モバイル専用カードレイアウト（`.mobile-table-cards`）」を [App.jsx](file:///c:/gemini_cli/得意先別商品検索ツール/src/App.jsx) に適用。
  - プロジェクト内に用意されていたがインポート漏れしていた [mobile-table.css](file:///c:/gemini_cli/得意先別商品検索ツール/src/mobile-table.css) を [main.jsx](file:///c:/gemini_cli/得意先別商品検索ツール/src/main.jsx) でインポートし、メディアクエリ（画面幅 768px 以下）との完全な連動を有効化。
- **目に優しいプレミアムビジネスカラーへのテーマ刷新**
  - 長時間の使用による目への負担を減らすため、原色に近い青（`#1E40AF`）から彩度を抑えた深みのあるスレートインディゴ（`#2B4C7E`）にテーマカラーを調整。
  - ボディの背景色を明るすぎるグレー（`#EAEDED`）から、目に優しい柔らかいブルーグレーのペールスチール（`#F1F5F9`）に変更。
  - テキスト色を漆黒から柔らかいダークスレートチャコール（`#1E293B`）に調整し、白背景での強いコントラストによるチラつき（眩しさ）を軽減。
- **サイドバーフィルターの操作性および視覚的フィードバックの改善**
  - 各チェックボックス行のクリック範囲（上下左右のパディング）を広げ、行全体をクリックしてトグルできるように改善。
  - ホバー時にごく薄い青のハイライト背景（`rgba(59, 130, 246, 0.08)`）と角丸を適用し、クリック可能な領域であることを明示。
  - 選択中のフィルター項目を太字と薄い背景で強調。
  - 各フィルター項目の該当件数を右端に揃えることでリスト全体の整列感を向上。
- **本番環境における Excel 解析時の Worker 起動エラーの修正**
  - プロダクションビルド環境（Vercel）において、Vite が Web Worker 内の SheetJS 依存関係を正しく解決できずに `Worker infrastructure error` でクラッシュしていた問題を修正。
  - Web Worker への依存を廃止し、[useProductData.js](file:///c:/gemini_cli/得意先別商品検索ツール/src/hooks/useProductData.js) にて直接 SheetJS のパースを実行する形に移行。
  - 解析処理を `Promise` & `setTimeout` を用いた非同期マイクロタスクで実行することで、解析中の UI フリーズを回避し、接続中の進捗状態が確実に描画されるよう改善。
  - 不要になった `src/workers/excelWorker.js` をプロジェクトから削除。
- **エクスポートされたExcel内での画像アスペクト比の維持と中央寄せ配置の修正**
  - エクスポートしたExcelファイル内で、商品画像が一律正方形（72x72px）に縮小されて歪んでしまう問題を修正。
  - 画像Blobからサイズ情報を非同期で解析する `getImageDimensions` ヘルパーを新設し、元のアスペクト比を保ったまま最大 `72px` ボックス内に縮小するロジックを実装。
  - ExcelJS の配置オフセット（`colOff` / `rowOff` を EMU 単位で計算）を指定することで、画像をセルの中央に綺麗に配置。
- **プライマリボタンの文字視認性の向上**
  - ダークモードとライトモードの両方で、プライマリボタン（ファイル選択・アップロード、顧客フォルダ接続など）の文字色とアイコン色が背景の青と同化して見づらかった問題を修正。
  - `.amazon-btn-primary` のテキストカラーを `white` (`#ffffff`) に統一し、ダークモード（スチールブルー背景）とライトモード（ディープネイビー背景）におけるアクセシビリティと視認性を改善。

## [Unreleased] - 2026-07-16

### Added
- **ビジネス向けプレミアムコーポレートテーマの適用**
  - ディープネイビー、スチールブルー、クリーングレーを基調とした、信頼感と知性を感じさせるカラーパレットに刷新。
  - すりガラス調の背景ぼかし（`backdrop-filter`）、ソフトなドロップシャドウ、滑らかなトランジションを伴う「ネオ・グラスモーフィズム」スタイルの採用。
  - ヘッダーへのライト/ダークテーマ切替トグルの追加と `localStorage` でのテーマ永続化。
- **インクリメンタル・サジェスト（オートコンプリート）機能の追加**
  - 検索窓入力中にタイトル、商品コード、受注Noに部分一致するユニークな候補リスト（最大8件）をリアルタイム表示。
  - キーボードの上下矢印キーでの選択およびEnterキーによる決定に対応。
- **検索履歴機能の追加**
  - 最近検索したクエリ（最大8件）を `localStorage` で保存。検索窓フォーカス時に一覧表示し、ワンクリック再検索や個別削除が可能。
- **詳細検索スコープ機能の追加**
  - 検索入力の左側に「すべて」「タイトル」「コード（商品コード/受注No/JAN）」の切り替えセレクトを追加し、ファジー検索のノイズを低減。
- **ファセットカウント付き複数選択式チェックボックス・アコーディオンフィルターの追加**
  - サイドバーのフィルターをプルダウンから複数選択可能なチェックボックスリスト形式に変更。
  - 各項目横に、その項目を選択した場合に何件該当するかを示す「ファセット数」をリアルタイム動的計算して表示。
  - カテゴリをアコーディオン形式にして折りたたみや個別クリアに対応。
- **品質とテストの強化**
  - テストフレームワーク `vitest` を導入し、`useProductFilters.test.js` でフィルタリング・ファセット計算等のユニットテストを作成。
  - 主要な新規・変更関数に JSDoc による型注釈と戻り値の明示を追加。

- **顧客データフォルダ接続およびキーワード検索・切り替え機能の追加**
  - File System Access API を使用して、顧客のExcelファイル（`.xlsx` / `.xls`）が格納されたローカルフォルダを接続・永続化する機能を追加。
  - アプリ起動時に前回のフォルダとの再接続を試み、接続完了時にファイル一覧を自動取得する処理を追加。
  - サイドバーおよびデータ未読み込み時のエンプティステート画面に、部分一致で顧客ファイルを絞り込める「顧客名検索」UI を実装。
  - リストから顧客名（ファイル名）を選択することで、該当するExcelファイルを自動でパースして商品データを切り替える機能を実装。

- **スマホ等（API非サポート環境）での複数顧客ファイル選択フォールバック対応の追加**
  - File System Access API が動作しない環境（スマホのiOS Safariなど）向けに、`input type="file" multiple` を使用して Dropbox や iCloud Drive から一括で顧客Excelファイルを選択し、メモリ上にロードできる機能を実装。
  - 非対応環境では顧客フォルダ接続ボタンの表記やプレースホルダーテキストを「顧客ファイル」に自動切り替え。

- **アプリ全体のUI/UXプレミアム化とアニメーションの快適化**
  - ボタンのホバー・アクティブ時のトランジションを滑らかに調整。角丸を `8px`〜`12px` に拡張し、影をソフトに再デザインして高級感を演出。
  - 商品カードのホバー時に、バウンス効果のある立体的な浮き上がりエフェクト（`transform` と `box-shadow`）を追加。
  - カート追加時にヘッダーの「カート」ボタンがプチバウンスするインタラクティブなアニメーションフィードバックを追加。
  - データロード待ち時間（`isLoading` が `true` のとき）に表示されるスケルトンローダー（Pulsing Skeleton Cards）を実装。
  - グリッド/テーブル表示モードの切り替え時にふわっとフェードイン＆スライドアップする切り替えアニメーション（`fade-in-up`）を追加。
  - 詳細モーダル出現時のフェード＆ズームアニメーション、および画像ギャラリー切り替え時の滑らかなフェードトランジションを追加。
  - 顧客リストおよびサイドバーのスクロールバーをスタイリッシュな極細スクロールバーにカスタマイズ。

### Improved
- **顧客切り替え時のページリセットの適正化**
  - ページネーションで2ページ目以降が表示されている状態で別の顧客が選択された（データ `data` が更新された）際、自動的に1ページ目にリセットされるように修正。
- **Excelファイル解析ロジックの共通化とリファクタリング**
  - 手動ファイルアップロード（`handleFileUpload`）と顧客フォルダ接続からの自動ファイル読み込み（`loadCustomerFile`）で重複していたExcelパース＆Web Worker呼び出し処理を `processExcelFile` に集約・共通化。
  - 各関数に JSDoc による詳細な型ヒント（Type Hints）と明示的な戻り値の型を追加。

## [Unreleased] - 2026-05-22

### Fixed
- **本番環境（Vercel）等での HTML 誤キャッシュによる画像エラーの解消**
  - 本番環境での SPA rewrite ルール（存在しないパスへのリクエスト時に `index.html` が返る設定）により、開発サーバー画像フェッチが HTML を返してしまい、それを正常な画像 Blob として誤認・キャッシュしてしまう問題を解消。
  - キャッシュ取得時において、データが `Blob` インスタンスかつ `type` が `image/` から始まっていることを厳格に検証する処理を追加。これにより、すでに保存されている不正キャッシュを自動検知してフォールバックするように修正。
  - 開発サーバーフェッチ時において、レスポンスの `Content-Type` ヘッダーが `image/` から始まっていることを検証し、HTML データ等の誤キャッシュを完全に防止。

- **画像読み込みエラー (Failed to load image: blob:) の解消**
  - `ProductImage.jsx` において、`useEffect` が再実行されるタイミングで表示中の `blob:` URL が同期的に `revoke` されてしまい、画像がロードできないバグを修正。
  - 非同期処理（キャッシュロード、ローカル配信フェッチ、File System API）の最中にコンポーネントがアンマウントまたは依存関係が変更された場合のキャンセル処理（`isCancelled` フラグ）を導入。
  - ステート更新（`updateImageUrl`）時およびアンマウント時にのみ古い `blob:` URL を安全に `revoke` する設計にリファクタリング。
  
- **詳細モーダルの画像チェック時のメモリリークおよびロード競合の解消**
  - `ProductDetailsModal.jsx` 内の非同期画像存在確認処理（`checkImages`）に `isCancelled` フラグを導入し、古い非同期処理が残した不要な `blob:` URL が画面に反映されたりリークしたりするのを防止。
  - モーダルアンマウント時およびステート更新時に古いローカル画像 URL を確実に `revoke` するようにライフサイクル管理を修正。

### Added
- **コード品質の向上とルール準拠**
  - 修正した `ProductImage.jsx` と `ProductDetailsModal.jsx` の関数やプロパティに JSDoc コメントによる詳細な型ヒント（Type Hints）と明示的な戻り値の型を追加。
  - 日本語コメント、英語コードのルールに完全準拠。

## [Unreleased] - 2026-05-21

### Added
- **画像フォルダの自動紐づけ機能 (ローカル開発サーバー自動連携)**
  - Vite開発サーバーにカスタムミドルウェア (`/_local_images`) を追加し、Windowsのネットワーク共有フォルダ `\\ASAHIPACK01\asahipac01_F\画像` から画像を自動的にブラウザに配信する仕組みを構築。
  - 開発者は手動で画像フォルダを選択することなく、シームレスにローカル画像が表示されるように改善。
  - パストラバーサル防止によるセキュリティ対策および拡張子フォールバック処理をミドルウェア側に実装。

- **画像フォルダ接続（FileSystemAccess API）の永続化と自動復元**
  - 手動で選択された `dirHandle` (ディレクトリハンドル) を `idb-keyval` (IndexedDB) に保存・永続化する機能を追加。
  - アプリ起動時に IndexedDB からハンドルを自動復元。
  - すでに保存されたハンドルが存在する場合、フォルダ接続ボタンを押した際に自動的に権限（`requestPermission`）のみを要求するようにし、フォルダ選択画面から再選択する手間を削減。

- **コード品質の向上とルール準拠**
  - `ProductImage.jsx` および `useProductData.js` 内の主要な非同期処理に JSDoc 型ヒント (`@returns`, `@param` 等) を追加。
  - コメントを日本語、コード記述を英語にするルールを徹底。
