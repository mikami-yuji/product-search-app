# 学んだ知見・開発教訓（Lessons Learned）

本プロジェクト（得意先別商品検索ツール）の開発・スマホ対応において得られた重要な技術的知見、Google Drive実データ仕様、および行動教訓の記録です。

---

## 🚨 1. 最重要行動教訓（推測の排除と事実確認）

- **推測で回答しない**: 認証画面やアクセス制約等で外部データを取得・読み取れていない場合、推測や当て推量で結果を報告してはならない。
- **一次情報の現場確認**: 事実が不明な場合は正直に報告し、検証ツール（ブラウザサブエージェント等）で実際の DOM や画面構造を確認した上で根拠を持って対応する。

---

## 📁 2. Google Drive 実データ構造と画像命名規則

Google Drive 共有フォルダ内の実在データ確認によって判明した確定仕様：

### 顧客サブフォルダの構成
- **命名規則**: `顧客コード_顧客名` （例: `16152_トーベイ（株）`, `27099_（株）みどりフーズ`, `22072_（株）中尾米穀店`）
- 先頭の数字（`16152`, `27099` 等）が各顧客の固有識別コード（`extractCustomerCode` で抽出可能）。

### 画像ファイルの命名規則
- **命名規則**: `受注№（数字5桁）+ アルファベット枝番（A, B, C, a, b...）+ .jpg`
  - 実在例: `44884A.jpg`, `44885A.jpg`, `60209C.jpg`
- **必須ロジック**: Excelデータの受注№が `44884` の場合、`44884A`, `44884B`, `44884C`, `44884a` 等の大文字・小文字アルファベット枝番サフィックスを自動補完・生成して最優先探索する（`generateOrderNoVariants`）。

---

## 📱 3. スマホ環境（iOS Safari / Android Chrome）の技術制約と対策

### ① File System Access API 非対応と input 起動
- スマホブラウザでは `window.showDirectoryPicker` および `<input webkitdirectory>` が使用不能（ダイアログが開かない、または不発）。
- **対策**: スマホ（`!isFileSystemSupported`）のときは、`<input type="file" multiple accept="image/*,.jpg,.jpeg,.png">` (`image-files-input` / `customer-files-input`) を優先起動する。

### ② ファイルパス消失とアンダースコア区切りファイル名対応
- スマホでのマルチファイル選択時、`file.webkitRelativePath` が入らず単一 `File` の配列になる場合がある。
- また、ファイル名が `27099_1005235.jpg` や `みどりフーズ_1005235.jpg` のようにアンダースコア `_` やハイフン `-` で連結される場合がある。
- **対策**: ファイル名から `_` や `-` で区切られた末尾の受注№単体（`1005235`）を自動抽出してメモリマップ（`imageFilesMap`）に多重登録する。

### ③ メモリ（RAM）と IndexedDB 保存による「もっさり感（フリーズ）」の完全解消
- **探索の高速化**: `imageFilesMap` からの画像探索で O(N) リニアスキャン（全件ループ）を行うとスクロールが劇的にラグくなる。O(1) ハッシュ検索（`Map.get()`）へ完全変更。
- **非ブロッキング書き込み**: 大容量データやファイル一覧を IndexedDB に書き込む際、`await set(...)` すると UI が数秒フリーズする。`Promise` によるバックグラウンド保存（UI切り替え優先）を採用。
- **軽量キャッシュ**: スマホ環境では `File` オブジェクトそのものを IndexedDB にシリアライズせず、軽量なファイル名リスト（テキスト）のみを保存する。

---

## 🎨 4. スマホUI/UX最適化ノウハウ

- **親アコーディオン統合**: 「顧客選択」と「直送先選択」を1つの親パネル「顧客・直送先選択」に集約し、折りたたみ可能にする。
- **サマリーバッジの提示**: パネルが閉じている状態でも選択中顧客名や直送先名をヘッダーにバッジ表示し、開かなくても状態が分かるようにする。
- **接続状態・取得元フォルダバッジ**: スマホ環境に限り、「画像接続済」ボタンの直下に `取得元: 16152_トーベイ` というミニバッジを表示し、接続状況を一目で把握可能にする。
